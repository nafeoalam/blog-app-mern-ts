{
  "name": "blog-app-mern-ts",
  "version": "1.0.0",
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "initiate": "npx lerna add scope && npx lerna bootstrap",
    "start": "npx lerna run start --scope={frontend,backend}"
  }
}
